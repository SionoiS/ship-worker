//use crate::id_types::ModuleId;
//use crate::cooldown_system::ActiveCooldowns;

/*pub fn trigger(modules: &ActiveCooldowns, module_id: ModuleId) {
    if !modules.on_cooldown(module_id) {
        //send start cooldown
    }
}*/